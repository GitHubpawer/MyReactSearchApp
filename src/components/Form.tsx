const Form = () => {
    return (
        <form>
            <input type="text" name="keyword" placeholder="Search for an image" />
            <button type="submit">Search</button>
        </form>
    )
};

export default Form;